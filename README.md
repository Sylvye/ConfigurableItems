# ConfigurableItems

Paper plugin for in-game editing of configurable custom items.

## Requirements

- Java 21

## Build

```sh
./gradlew build
```

On Windows:

```bat
gradlew.bat build
```

The plugin jar is written to `build/libs/`.
